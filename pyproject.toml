[project]
name = "nautilus-ibkr"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "nautilus-trader[docker,ib,visualization]>=1.231.0",
    "pandas>=2.3",
    "pyarrow>=25.0",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false
